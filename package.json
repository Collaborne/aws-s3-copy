{
  "name": "aws-s3-copy",
  "version": "1.0.3",
  "description": "Helper to copy S3 objects within the same bucket",
  "main": "dist/s3-copy.js",
  "types": "dist/s3-copy.d.js",
  "repository": "https://github.com/Collaborne/aws-s3-copy.git",
  "author": "Collaborne B.V.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json \"src/**/*.ts\"",
    "prepublish": "npm run lint && npm run build"
  },
  "devDependencies": {
    "@types/node": "^10.12.10",
    "tslint": "^5.11.0",
    "tslint-config-collaborne": "^0.0.4",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "aws-sdk": "^2.362.0",
    "urijs": "^1.19.1"
  }
}
